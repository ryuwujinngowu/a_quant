dataset.py __main__
│
├─ 1. SectorHeatFeature.select_top3_hot_sectors(date)
│      → top3_sectors + adapt_score
│
├─ 2. 构建 sector_candidate_map
│      板块过滤 → ST过滤 → 涨停基因过滤
│
├─ 3. FeatureDataBundle(... adapt_score=adapt_score)
│      → 一次性预加载日线 + 分钟线，所有因子共享
│
├─ 4. FeatureEngine.run_single_date(data_bundle)
│      ├─ SectorHeatFeature.calculate()   读 bundle.adapt_score → [trade_date, adapt_score]
│      ├─ SectorStockFeature.calculate()  读 bundle.daily/minute → 个股级所有因子
│      └─ 合并：个股级 inner join，全局级 left join broadcast
│         → feature_df（含 adapt_score、candle_type、hdi 等所有列）
│
├─ 5. LabelEngine.generate_single_date()
│      → label_df（label1, label2）
│
├─ 6. merge(feature_df, label_df) → 清洗 → 追加写 CSV
│
└─ 7. ProcessedDatesManager.add(date)  写入成功才标记，保证幂等